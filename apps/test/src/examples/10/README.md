# 10

Packed (`$graph` array) document mirroring a StageIn+StageOut service: one Workflow (single `Directory` input) whose only step runs the packed `#main` CommandLineTool (Docker + NetworkAccess + EnvVar requirements). The string variant additionally carries the `$namespaces`/`$schemas` metadata blocks.
