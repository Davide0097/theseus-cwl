{
  "name": "theseus-cwl",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=@theseus-cwl/configurations",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "publish-packages": "turbo run build --filter=@theseus-cwl/configurations lint test && changeset version && changeset publish"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.4.4",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.29.5"
  }
}
